spring:
  application:
    name: Curd

  datasource:
    url: jdbc:mysql://localhost:3306/curd?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: XZY123456789
    driver-class-name: com.mysql.cj.jdbc.Driver

  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java
      exclude: static/**

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml


